CXX_STD = CXX11

PKG_LIBS =  $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(SHLIB_OPENMP_CFLAGS) $(pkg-config --libs opencv)
PKG_CFLAGS = $(SHLIB_OPENMP_CFLAGS) $(pkg-config --cflags opencv)
PKG_CPPFLAGS = $(SHLIB_OPENMP_CFLAGS) $(pkg-config --cflags opencv)
PKG_CXXFLAGS = $(SHLIB_OPENMP_CFLAGS) $(pkg-config --cflags opencv)